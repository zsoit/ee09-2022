{
    "dependencies": {
        "jstransformer-coffee-script": "^1.1.1",
        "jstransformer-markdown-it": "^2.1.0",
        "pug": "^3.0.2"
    },
    "scripts": {
        "dev": "npx parcel index.pug",
        "github": "cp -rf dist/* docs/ && git add . && git commit -m 'update && git push'"
    },
    "devDependencies": {
        "@parcel/transformer-pug": "^2.0.1",
        "parcel": "^2.0.1"
    }
}